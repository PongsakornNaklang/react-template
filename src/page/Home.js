import React from 'react'
// import { useGlobalVar } from '../context/global_var'

export const Home = () => {
    // const { user_info } = useGlobalVar();
    // const [userinfo, setUserInfo] = user_info

    return (
        <div>
            <h1>Home page</h1>
        </div>
    )
}
